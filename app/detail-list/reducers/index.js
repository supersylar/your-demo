const initialState = {
  detailList: [
    {
      name: 'detailList 1',
      id: 10001
    }
  ]
}

const detailListReducer = (state = initialState, action) => {
  return state
}

export default detailListReducer